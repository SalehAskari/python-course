## ۱. مفهوم دنباله (Sequence) و ایندکس (Index)

در پایتون، بعضی ساختارها **دنباله** هستند: مثل _رشته، لیست، تاپل_.
یعنی عناصرشان _ترتیب_ دارند و می‌توانی با شماره (ایندکس) به آن‌ها دسترسی بگیری.

### 📌 ایندکس چیست؟

ایندکس یعنی شمارهٔ هر عنصر در یک دنباله:

```
 H   e   l   l   o
 0   1   2   3   4
```

هم ایندکس مثبت داریم هم منفی:

- مثبت → از اول
- منفی → از آخر

```python
s = "python"
print(s[0])   # p
print(s[-1])  # n
```

### 📌 اسلایس (slice)

برای گرفتن بخش‌هایی از یک دنباله:

```python
s = "python"
print(s[1:4])   # yth
print(s[:3])    # pyt
print(s[::2])   # pto
```

---

### ❓ سؤال برای فهم بهتر (بخش ایندکس)

رشتهٔ زیر را در نظر بگیر:

```python
msg = "Networking"
```

با استفاده از ایندکس و اسلایس، خروجی‌های زیر را بساز:

1. `N`
2. `ing`
3. `work`
4. رشته را طوری اسلایس کن که فقط حروف ایندکس‌های زوج چاپ شود.

---

## ۲. لیست (List)

لیست مهم‌ترین ساختار داده مبتنی بر دنباله است: **قابل تغییر (mutable)**.

### ساخت لیست

```python
lst = [10, 20, 30, "hi", True]
```

### دسترسی

```python
print(lst[0])   # 10
print(lst[-1])  # True
```

### تغییرپذیری

```python
lst[1] = 999
print(lst)  # [10, 999, 30, "hi", True]
```

### متدهای مهم لیست

| متد            | توضیح                      |
| -------------- | -------------------------- |
| `append(x)`    | اضافه کردن به آخر          |
| `insert(i, x)` | افزودن در ایندکس مشخص      |
| `pop(i)`       | حذف و بازگرداندن           |
| `remove(x)`    | حذف اولین مقدار برابر با x |
| `clear()`      | خالی کردن                  |
| `sort()`       | مرتب‌سازی                  |
| `reverse()`    | معکوس کردن                 |

### مثال

```python
nums = [4, 1, 9]
nums.append(5)         # [4,1,9,5]
nums.sort()            # [1,4,5,9]
nums.pop(2)            # خروجی: 5  لیست: [1,4,9]
```

---

### ❓ سؤال لیست

لیست زیر را در نظر بگیر:

```python
a = [3, 1, 4]
```

کاری کن که نتیجهٔ نهایی لیست به شکل زیر باشد:

```
[1, 3, 4, 10, 20]
```

قواعد:

- ابتدا لیست را مرتب کن
- عدد 10 را به آخر اضافه کن
- عدد 20 را در ایندکس **3** قرار بده

---

## ۳. تاپل (Tuple)

تاپل مثل لیست است اما **قابل تغییر نیست**.

### ساخت

```python
t = (10, 20, 30)
```

### دسترسی (مثل لیست)

```python
print(t[0])   # 10
```

### ویژگی‌ها

- غیرقابل تغییر
- سبک‌تر از لیست
- قابل استفاده به عنوان کلید دیکشنری
- مناسب داده‌های ثابت

### متدهای تاپل

- `count(x)`
- `index(x)`

### مثال

```python
data = (2, 5, 2, 9)
print(data.count(2))  # 2
print(data.index(9))  # 3
```

---

### ❓ سؤال تاپل

تاپل زیر را در نظر بگیر:

```python
t = (10, 20, 30, 40, 50)
```

کاری کن که فقط **مقادیر وسطی** (۲۰، ۳۰، ۴۰) را با استفاده از _اسلایس_ استخراج کنی.

---

## ۴. دیکشنری (Dictionary)

دیکشنری ساختار دادهٔ **کلید → مقدار** است.

### ساخت

```python
info = {"name": "Ali", "age": 25}
```

### دسترسی

```python
print(info["name"])
print(info.get("job", "not found"))
```

### تغییر مقدار

```python
info["age"] = 30
```

### افزودن کلید جدید

```python
info["city"] = "Tehran"
```

### متدهای مهم

| متد         | توضیح          |
| ----------- | -------------- |
| `keys()`    | لیست کلیدها    |
| `values()`  | مقادیر         |
| `items()`   | جفت کلید-مقدار |
| `update()`  | بروزرسانی      |
| `pop(key)`  | حذف            |
| `popitem()` | حذف آخرین جفت  |

مثال:

```python
user = {"a": 1, "b": 2}
user.update({"b": 10})  # {'a':1,'b':10}
```

---

### ❓ سؤال دیکشنری

دیکشنری زیر داده شده:

```python
scores = {"Ali": 18, "Sara": 20, "Reza": 12}
```

کاری کن که فقط نام دانش‌آموزانی که نمره‌شان بالای 15 است در یک **لیست** قرار بگیرد.

---

## ۵. ست (Set)

ساختار **بدون تکرار** و **بدون ترتیب**.

### ساخت

```python
s = {1, 2, 3}
```

### ویژگی‌ها

- عناصر تکراری حذف می‌شوند
- ایندکس ندارد
- عملیات مجموعه‌ای انجام می‌دهد

### عملیات مهم

```python
a = {1, 2, 3}
b = {3, 4, 5}

a | b     # اجتماع → {1,2,3,4,5}
a & b     # اشتراک → {3}
a - b     # تفاضل → {1,2}
```

### متدها

- `add(x)`
- `discard(x)`
- `remove(x)`
- `union()`, `intersection()`
- `isdisjoint()`

---

### ❓ سؤال ست

دو مجموعه زیر را داری:

```python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}
```

خروجی‌های زیر را بساز:

1. فقط اعدادی که در **هر دو** هستند
2. اعدادی که در **فقط یک مجموعه** حضور دارند
3. اعدادی که در a هستند ولی در b نیستند