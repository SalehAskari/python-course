## 1️⃣ تابع چیست و چرا از آن استفاده می‌کنیم؟

تابع (Function) یک بلوک کد نام‌دار است که برای انجام یک کار مشخص نوشته می‌شود و هر زمان که لازم داشته باشیم می‌توانیم آن را صدا بزنیم.
در برنامه‌نویسی، استفاده از تابع باعث می‌شود:

- کدها تکراری نوشته نشوند
- برنامه خواناتر و منظم‌تر شود
- نگهداری و توسعه برنامه ساده‌تر گردد
- مسائل بزرگ به بخش‌های کوچک‌تر تقسیم شوند

### مثال ساده از تعریف و فراخوانی تابع

```python
def say_hello():
    print("Hello, welcome to Python class!")

say_hello()
```

🔹 کلمه کلیدی `def` برای تعریف تابع استفاده می‌شود
🔹 بدنه تابع با تورفتگی مشخص می‌شود

---

## 2️⃣ تابع با آرگومان (Parameter / Argument)

آرگومان‌ها داده‌هایی هستند که هنگام فراخوانی به تابع ارسال می‌کنیم تا تابع روی آن‌ها عملیات انجام دهد.

### مثال: تابع با یک آرگومان

```python
def greet(name):
    print("Hello", name)

greet("Ali")
greet("Sara")
```

---

## 3️⃣ تابع با چند آرگومان

```python
def add(a, b):
    print("Sum is", a + b)

add(3, 5)
add(10, 20)
```

🔹 ترتیب آرگومان‌ها اهمیت دارد

---

## 4️⃣ مقدار بازگشتی (return)

گاهی هدف تابع فقط چاپ نتیجه نیست، بلکه می‌خواهیم **مقداری تولید کند و به برنامه برگرداند**.

```python
def multiply(a, b):
    return a * b

result = multiply(4, 5)
print("Result is", result)
```

🔹 بعد از اجرای `return`، تابع متوقف می‌شود

---

## 5️⃣ تفاوت print و return (نکته بسیار مهم)

```python
def func1():
    print("Hello")

def func2():
    return "Hello"

x = func1()
y = func2()

print("x is", x)
print("y is", y)
```

📌 خروجی:

```
Hello
x is None
y is Hello
```

🔹 `print` فقط نمایش می‌دهد
🔹 `return` مقدار قابل استفاده در ادامه برنامه تولید می‌کند

---

## 6️⃣ آرگومان پیش‌فرض (Default Argument)

```python
def power(base, exp=2):
    return base ** exp

print("Result is", power(3))
print("Result is", power(3, 3))
```

🔹 اگر آرگومان داده نشود، مقدار پیش‌فرض استفاده می‌شود

---

## 7️⃣ آرگومان‌های کلیدی (Keyword Arguments)

```python
def student_info(name, age):
    print("Name:", name)
    print("Age:", age)

student_info(age=20, name="Reza")
```

🔹 ترتیب در این حالت مهم نیست

---

## 8️⃣ آرگومان‌های متغیر (\*args)

زمانی استفاده می‌شود که تعداد ورودی‌ها مشخص نیست.

```python
def average(*numbers):
    total = sum(numbers)
    return total / len(numbers)

print("Average is", average(10, 20, 30))
print("Average is", average(5, 15))
```

🔹 `numbers` یک `tuple` است

---

## 9️⃣ آرگومان‌های کلیدی متغیر (\*\*kwargs)

زمانی که نمی‌دانیم تابع چه تعداد آرگومان **نام‌دار** دریافت می‌کند، از `**kwargs` استفاده می‌کنیم.

```python
def show_info(**kwargs):
    print("Data:", kwargs)

show_info(name="Ali", age=20, city="Tehran")
```

📌 خروجی:

```
Data: {'name': 'Ali', 'age': 20, 'city': 'Tehran'}
```

🔹 داده‌ها به صورت **دیکشنری** در اختیار تابع قرار می‌گیرند

---

## 🔟 تفاوت \*args و \*\*kwargs

| مورد       | نوع داده |
| ---------- | -------- |
| `*args`    | tuple    |
| `**kwargs` | dict     |

### مثال ترکیبی

```python
def test(*args, **kwargs):
    print("args:", args)
    print("kwargs:", kwargs)

test(1, 2, 3, name="Sara", score=18)
```

---

## 1️⃣1️⃣ ترتیب صحیح آرگومان‌ها (نکته امتحانی)

```python
def func(a, b=2, *args, **kwargs):
    pass
```

ترتیب درست:

1. آرگومان‌های معمولی
2. آرگومان‌های پیش‌فرض
3. `*args`
4. `**kwargs`

---

## 1️⃣2️⃣ متغیرهای محلی (Local Variables)

```python
def test():
    x = 10
    print("Inside function:", x)

test()
```

🔹 متغیر `x` فقط داخل تابع قابل دسترسی است

---

## 1️⃣3️⃣ متغیرهای سراسری (Global Variables)

```python
x = 5

def show():
    print("x is", x)

show()
```

---

## 1️⃣4️⃣ تفاوت متغیر محلی و سراسری

```python
x = 10

def change():
    x = 20
    print("Inside function:", x)

change()
print("Outside function:", x)
```

---

## 1️⃣5️⃣ استفاده از global

```python
x = 10

def change():
    global x
    x = 20

change()
print("x is", x)
```

⚠️ استفاده زیاد از `global` توصیه نمی‌شود

---

## 1️⃣6️⃣ تابع بازگشتی (Recursive Function)

تابعی که خودش را صدا می‌زند.

### مثال: فاکتوریل

```python
def factorial(n):
    if n == 1:
        return 1
    return n * factorial(n - 1)

print("Factorial is", factorial(5))
```

🔹 شرط توقف (Base Case) ضروری است

---

## 1️⃣7️⃣ مثال ساده‌تر از بازگشتی

```python
def countdown(n):
    if n == 0:
        return
    print("Number:", n)
    countdown(n - 1)

countdown(5)
```

---

## 1️⃣8️⃣ تابع تو در تو (Nested Function)

```python
def outer():
    def inner():
        print("Inside inner function")
    inner()

outer()
```

---

## 1️⃣9️⃣ مستندسازی تابع (Docstring)

```python
def add(a, b):
    """
    This function adds two numbers
    """
    return a + b

print(add.__doc__)
```

---

## 🔚 جمع‌بندی نهایی

در این درسنامه:

- مفهوم تابع و مزایای آن بررسی شد
- انواع آرگومان‌ها (`normal`، `default`، `*args`، `**kwargs`) پوشش داده شد
- تفاوت `print` و `return` توضیح داده شد
- متغیرهای محلی و سراسری بررسی شدند
- توابع بازگشتی با مثال تدریس شدند

---

اگر بخواهی، می‌توانم:

- 📄 همین متن را **PDF یا Word**
- 🎯 همراه با **تمرین کلاسی و چالشی**
- 📝 یا به صورت **اسلاید تدریس فردا**

برایت آماده کنم.
بگو دقیقاً برای کلاس فردا چی لازم داری 👨‍🏫📚
